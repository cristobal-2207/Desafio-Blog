<h1>Posts#new</h1>

<%= form_with model: @post, url: posts_create_path do |f| %>
  <% if @post.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this task from being saved:</h2>
	      <ul>
		      <% @post.errors.full_messages.each do |msg| %>
		        <li><%= msg %></li>
		      <% end %>
	      </ul>
	    </div>
	<% end %>
  <%= f.text_field :title %>
  <%= f.date_field :date %>
  <%= f.text_field :image_url %>
	<%= f.text_area  :content %>
	<%= f.submit 'Guardar' %>
<% end %>